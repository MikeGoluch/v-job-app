import React from "react";
import Button from "../../UI/Button/Button";

const job = (props) => {
  return (
    <div>
      <p>{props.job}</p>
      {/* <Button>Apply</Button> */}
    </div>
  );
};

export default job;
